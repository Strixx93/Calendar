specVersion: "v1"
serviceId: "my-calendar-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-calendar-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
